<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.turlir.abakgists">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".base.App"
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="false"
        android:theme="@style/AppTheme">

        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".allgists.AllGistsActivity" android:label="@string/all_gists"/>

        <activity android:name=".GistActivity" android:label="@string/gist"/>

        <activity android:name=".notes.NotesActivity" android:label="@string/notes"/>

        <activity android:name=".AllInOneActivity" android:theme="@style/AllInOneTheme"
            android:label="@string/all_in_one"/>

    </application>

</manifest>